// Store to track last saved states per document
const lastSavedStates = new Map();

// Store version file paths for each document
const documentVersions = new Map();

export { lastSavedStates, documentVersions };
